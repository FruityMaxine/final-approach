# FINAL APPROACH 独立项目 · 修改记录 2026-06-06

> 接 arcade 内 FA组10-11 历史(见 vega-arcade docs/progress)。本项目独立开源后,修改记录入此。

## FA组11 Tick3 (多发引擎模型 engines.js · repo v0.7.0.0)
**改了什么**：新增 web/engines.js 多发引擎模型,把"单一合集推力"重构为可配置多发(2/4发)独立模型:
- 每发独立 {n1,n2,egt,ff,state,throttleOvr,fuelCut,fire,starter,ign},状态机 off/start/idle/run/fail/fire,engineStep(dt) 推进各发 spool(N1惯性)/N2/EGT(随N1)/FF燃油流量;启动序列(starter→N2升→点火→idle→run)。
- 物理总推力 T = ENGINES.totalThrust() = Σ各发(失效发动机贡献≈0,总推力自然下降);反推地面取负。
- **不对称推力偏航**:ENGINES.yawFromThrust() 按各发横向力臂×推力求矩,接入 game.js 偏航 psiDot(非装饰)——单发失效把机头拉向死发一侧。
- EICAS 动态显示:renderEICAS() 按发动机数生成 2 或 4 列 N1 条+数值+EGT,失效列变红。
- 故障注入接口 failEngine/fireEngine/cutoff(供 Tick4 系统面板+组12故障系统调用)。
- 配置:发动机数(双发/四发)入飞行设置面板+CONFIG.engines+localStorage持久化;S.N1 取队均供 UI/EMMA/声音向后兼容。
**为什么改**：用户要硬核多发引擎独立控制。这是引擎/燃油/系统面板(Tick4-5)与故障系统(组12)的物理基础。
**影响范围**：新增 engines.js(~95行);game.js(物理 spool/推力/偏航改 + resetState引擎重置 + renderEICAS + syncRuntimeUI多发显示 + init setCount + 配置seg,~60行);config.js(CONFIG.engines+持久化);index.html(engines.js script + 发动机seg);game.css(.failed/.egtnum);game.json(modules+0.6→0.7.0.0)。
**实测**：Playwright — 双发/四发EICAS动态列(2/4 units);单发失效航向0°→13.24°偏向死发(yaw moment 0.169,确认偏航接物理非装饰);E1失效退转state=fail/n1=0.05;同油门总推力=Σ下降;零console错误;截图确认四发EICAS。

## FA组11 Tick4 (面板框架 PanelFramework + 引擎系统面板 · repo v0.8.0.0)
**改了什么**：新增 web/panels.js 面板框架 —— 不写死单面板,而是可注册数十种驾驶舱面板的框架:
- PANELS.register(id,{label,build,wire,sync}) 注册即生效,选择器 tab 条自动出条目;open(id)显示覆盖层,close()回飞行视图;SYS.panels 开关过滤面板可见性。
- 面板选择器 #panelBar(飞行 + 已注册面板,如 引擎)底部 tab 条;#panelHost 覆盖层覆于世界视图上(飞行继续),每帧 PANELS.sync() 刷新当前面板读数。
- 首个注册面板=引擎/燃油系统面板:每发一列(ENG 1..N),显示 state+N1/N2/EGT/FF,5 按钮(起动机/点火IGN/断油CUTOFF/灭火手柄/燃油泵)。
- **按钮真连动 engines.js 状态机**:CUTOFF→fuelCut停车、STARTER+IGN→启动序列(N2起转点火至慢车run)、灭火手柄→断油灭火,非纯显示。
**为什么改**：用户北极星"真飞机几十种面板都能做出来"——建框架而非一次性面板,引擎面板只是首个,MCDU/液压/电气等后续按框架无限加。
**影响范围**：新增 panels.js(~95行);engines.js(加pump字段);index.html(#panelBar/#panelHost+panels.js script);game.css(面板框架+引擎列+按钮样式~50行);game.js(PANELS.init+loop sync);game.json(modules+0.7→0.8.0.0)。
**实测**：Playwright — 选择器 tabs[飞行,引擎];开引擎面板 panelHost.show+2列+current=engine;CUTOFF eng1→state=off/n1=0.00(按钮连动状态机非装饰);重启序列(CUTOFF关+STARTER+IGN)→state=run/n2=0.63;零console错误;截图确认引擎面板硬核视觉。

## FA组11 Tick5 (燃油系统 fuel.js · repo v0.9.0.0 · 组11收官)
**改了什么**：新增 web/fuel.js 燃油系统:
- TANK{left,center,right}各{qty,cap}+pump泵+xfeed交输活门;FUEL.step(dt)按各发FF(engines.js)扣油,路由:中央先用→同侧机翼箱,crossfeed开则跨侧供油,泵关的箱不供油。
- **燃油饥饿真连锁**:某发供油不足(箱空+无crossfeed)→e.fuelStarve+state转fail(engines状态机熄火),非数字显示。
- 油量影响总重 AC.m=dryMass(44000)+总油量(满油18000→62000不变,烧油变轻)。
- 新增"燃油"面板(PANELS第2个注册面板,验证框架可扩):三油箱竖条synoptic+泵开关+交输活门+总量。
- SYS登记features.fuelSystem(关=无限油简化档)+panels.fuel。
**为什么改**：用户硬核燃油系统诉求(左中右油箱/泵/交输/耗油)。燃油饥饿熄火是引擎×燃油跨模块连锁,也为组12故障系统(燃油泄漏)铺路。
**影响范围**：新增fuel.js(~90行);game.js(AC.dryMass+FUEL.step/reset);engines.js(启动清fuelStarve);sys.js(fuelSystem+fuel面板登记);index.html(fuel.js script);game.css(油箱/交输样式~22行);game.json(modules+0.8→0.9.0.0)。
**实测**：Playwright — 面板tabs[飞行,引擎,燃油](框架第2面板);耗油递减;右+中空+交输关→eng2 fail+fuelStarve真熄火、eng1仍run(跨模块连锁);右+中空+交输开→eng2跨供left箱仍run;synoptic三油箱;零console错误;截图确认燃油面板。

## FA组12 Tick2 (故障核心 failures.js + ECAM面板 + 主警告灯 · repo v0.10.0.0)
**改了什么**：新增 web/failures.js 故障核心系统:
- FAILURES注册表:每故障{id,sys,msg,level(caution/warning),active,trigger(),clear(),step()},register/trigger/clear/toggle/activeList/hasWarning/hasCaution API,step(dt)推进+与SYS.failures双向联动(SYS开关↔故障active,为组15总控面板铺路)。
- 登记6种故障:engineFire/engineFail本tick接engines.js(e.fire/failEngine)验证连锁可行;fuelLeak/hydFail/elecFail/tireBurst留Tick3-5填逻辑。
- ECAM故障面板(PANELS第3个注册面板,框架再扩验证):各故障手动触发按钮(按级着色)+ECAM active告警滚动列表。
- 主警告(红warning)/主警戒(琥珀caution)双灯:viewport右上角,有active warning→灯闪+警告音(Sound.blip周期),caution→琥珀灯;点灯消音recall(故障消失自动复位);z-index 11永在面板覆盖层之上(真实驾驶舱主警告随时可见)。
**为什么改**：用户故障系统诉求的基础设施。FAILURES核心被Tick3-5复用,ECAM+主警告是所有故障的统一告警出口。
**影响范围**：新增failures.js(~95行);index.html(master双灯+failures.js script);game.js(FAILURES.step+updateMaster+警告音+recall);sys.js(panels.failures登记);game.css(主灯+ECAM+故障注入样式~30行,含z-index 11修复主灯被面板遮挡);game.json(modules+0.9→0.10.0.0)。
**实测**：Playwright — 4面板tab(飞行/引擎/燃油/故障);触发engineFire→e.fire=true+active+SYS联动+ECAM"ENG 1 FIRE"+主警告灯亮(真连锁);点灯消音→灯灭;解除→e.fire=false+ECAM清空+SYS=false;hydFail(caution级)→琥珀灯亮warn灯灭(分级正确);零console错误;截图确认ECAM面板+主警告灯。修复主灯z-index被面板遮挡(真UX:主警告永在最上)。

## FA组12 Tick3 (引擎故障真连锁 起火/失效/燃油泄漏 · repo v0.11.0.0)
**改了什么**：引擎类故障落地真连锁影响:
- **引擎起火**:engines.js engineStep加fire覆盖逻辑——e.fire时EGT飙升(+230/s至>900红线,1150封顶)+fireDmg损害累计,久烧>9s致state=fail;run分支EGT lerp加!e.fire守卫(火焰主导不被正常值拉回,修EGT拉锯bug)。failures.js engineFire.step:久烧>15s蔓延邻发(ENG 1+2 FIRE)。引擎面板"灭火手柄"fire按钮→e.fire=false灭火+断油停车(已有,本tick确保火焰逻辑配合)。
- **燃油泄漏**:fuel.js tank加leak字段+step每秒漏油;failures.js fuelLeak.trigger→左箱leak=10kg/s,step按总油量<2500升级caution→warning(msg "FUEL LOW · LEAK");漏到空→该侧发动机饥饿熄火(接已有flameout)。
- EGT>850在EICAS+引擎面板红色危险显示(.egtnum.hot/.eng-gauges b.hot)。
- engineFail停车+不对称偏航(已有)+ECAM。
**为什么改**：用户故障须真连锁非纯显示。起火→EGT/推力损害、漏油→油量流失→熄火,都是真实物理后果。
**影响范围**：engines.js(fire EGT飙升+损害+失效+EGT守卫,reset加fireDmg/fuelStarve);fuel.js(tank.leak+step漏油);failures.js(engineFire.step蔓延+fuelLeak连锁+level升级);game.js(EICAS EGT红);panels.js(引擎面板EGT红);game.css(.hot样式);game.json(0.10→0.11.0.0)。
**实测**：Playwright — engineFire→EGT飙升1042(>900)+久烧10s失效fail;拉灭火手柄→fire=false+断油;fuelLeak→左箱持续漏递减+抽空→左发fail饥饿熄火(真连锁);零console错误。修复起火EGT被run分支lerp拉回的拉锯bug。

## FA组12 Tick4 (液压系统 hyd.js · repo v0.12.0.0)
**改了什么**：新增 web/hyd.js 液压系统:
- HYD{A,B,C}三液压源各{press压力,pump泵,eng驱动源,failed}:A=主操纵(发1驱动)/B=扰流板·起落架(发2驱动)/C=刹车·备份(电动)。HYD.step(dt)压力渐变(蓄压器缓冲)。
- **操纵增益真连锁**:game.js俯仰S.pitchIn*9*hg、横滚S.rollIn*36*hg、脚舵rudder*0.42*hg 乘 HYD.ctrlGain()(系统A压力低→增益降至25%人力备份,操纵沉重迟钝);扰流板自动展开受HYD.spoilerOK()(B压力,失则不可展且收回);刹车效能fric*HYD.brakeGain()(C压力)。
- **cascade**:发动机失效→其驱动的液压泵失压(eng-driven pump,engineStep state非run→对应HYD源press降)。
- failures.js hydFail→HYD.fail('A')压力降+ECAM "HYD A LO PR"。
- 新增"液压"面板(PANELS第4个注册面板,框架再扩):三源压力条(失压变红)+psi+泵开关。
**为什么改**：用户故障须真影响操纵非显示。液压低→操纵沉重是真实飞行后果。
**影响范围**：新增hyd.js(~70行);game.js(操纵增益hg+扰流板/刹车gate+HYD.step/reset);failures.js(hydFail接HYD);index.html(hyd.js script);sys.js(panels.hyd);game.css(液压面板样式~14行);game.json(0.11→0.12.0.0)。
**实测**：Playwright — 5面板(飞行/引擎/燃油/液压/故障);hydFail→A压降0.17+俯仰速率5.55°→1.90°/0.6s(操纵增益真降可测);发动机1失效→A泵失压0.13(cascade);液压面板3源压力条;零console错误。

## FA组12 Tick5 (电气+PFD失效+爆胎+MTBF随机注入 · repo v0.13.0.0 · 组12收官)
**改了什么**：
- **电气系统elec.js**:ELEC{电池bat,发电机gen1/gen2(发动机驱动),busAC/DC汇流条}+step;genPower由对应发动机状态决定(非run→失电);pfdPower()/acPower()。新增"电气"面板(PANELS第6面板)电池/发电机/汇流条状态+开关。
- **PFD失效真连锁**:game.js drawPFD接ELEC——!pfdPower()→PFD黑屏+红X "ATT FAIL";!acPower()(电池备份)→PFD降亮度0.55运行(degraded)。
- **爆胎tireBurst**:game.js地面分支读FAILURES.reg.tireBurst.active→滑跑时S.hdg持续拉向爆胎侧(+14°/s)+S.x偏移。
- **MTBF随机故障注入**:FAILURES.randomInject(dt)按难度failRate(config.js DIFF:新手0/真实0.004/硬核0.02每秒)泊松随机触发未激活故障+大字告警;SYS.env.randomFailures开关gate;game.js loop调用(飞行中)。
- **cascade**:发动机失效→发电机失电(elec)+液压泵失压(已有);elecFail→总失电PFD黑屏。
- failures.js:elecFail接ELEC.fail(warning级),tireBurst物理读active,randomInject方法。
**为什么改**：用户故障系统收官——电气/PFD失效/爆胎真影响(PFD黑屏/地面拉偏)+随机故障涌现(硬核档),非纯显示。
**影响范围**：新增elec.js(~60行);game.js(ELEC.step/reset+drawPFD电气gate+爆胎物理+randomInject调用);failures.js(elecFail/tireBurst/randomInject);config.js(DIFF failRate);sys.js(env.randomFailures+panels.elec);index.html(elec.js);game.css(电气面板);game.json(0.12→0.13.0.0)。
**实测**：Playwright — 6面板(飞行/引擎/燃油/液压/电气/故障);elecFail→pfdPower=false+PFD黑屏红X(截图确认);双发失效→busAC=0+gen失电但PFD电池备份仍亮(cascade真实区分);爆胎→地面S.hdg拉偏18.56°;随机注入(硬核0.95/s)4s涌现4故障;零console错误。

## FA组13 Tick2 (修近平面裁剪bug · repo v0.14.0.0)
**改了什么**：修复用户最早报的"跑道在视野内却不渲染"——投影管线引入近平面多边形裁剪:
- render.js project()拆分:toCamera(px,py,pz)→{r,u,z}相机空间 + projectCam(c)→屏幕;project()=组合(z<=NEAR返null,向后兼容点剔除)。NEAR=1。
- clipPolyNear(相机空间顶点环):Sutherland-Hodgman近平面裁剪,跨NEAR的边按z插值切割,返回可见多边形(0/3/4/5点)。fillPoly3d/quad3d(世界坐标四点)→toCamera→裁剪→projectCam→填充。
- 跑道主面/标线/接地区/中线/田块/滑行道全改 quad3d(原quad(project()×4)整体丢弃→裁剪切割);worldBox逐面相机空间裁剪+深度排序;groundLine两点近平面裁剪。
**为什么改**：原 project camZ<=1返null + quad任一点null整体丢弃,飞机贴近跑道时近段四边形一角转到相机后→整段消失。多边形裁剪后跨近平面的面被切成可见部分继续填充。
**影响范围**：render.js(投影管线重构+裁剪+quad3d+worldBox/groundLine/drawTerrain/drawRunway/drawAirport改~90行);game.json(0.13→0.14.0.0)。21处project点调用(灯/文字/PAPI)保持向后兼容。
**实测**：Playwright — clipPolyNear跨近平面四边形→裁剪4点minz=1(NEAR);贴近跑道(S.z=10/alt=11/pitch=-12)下半屏4/4灰色采样(跑道面填满未穿透);截图确认近段跑道延伸到屏底不消失;常规进近视图非空不破;零console错误。

## FA组13 Tick3 (程序化世界 worldgen.js · repo v0.15.0.0)
**改了什么**：解决用户报的"建筑太少且仅机场附近"——程序化连续世界生成:
- 新增 web/worldgen.js:WORLDGEN网格cell(200m)+**per-cell独立hash**(rngFor(cx,cz)用 imul(cx,73856093)^imul(cz,19349663)→mulberry32确定性序列),同cell每帧完全一致不抖(非render.js全局序列)。
- 分带zone(wx,wz):近跑道走廊留空(ax<95)/城市核(ax<650,z -600..3600,高楼h≤115)/郊区(ax<1500,h≤38)/田野(稀树+地块)。cellsAround(camX,camZ,range)返相机周边建筑/树。
- render.js drawScenery改遍历WORLDGEN.cellsAround(替换固定46对象SCENERY),建筑用worldBox(已近平面裁剪),远近排序+距离剔除;城市道路网格(groundLine沿cell边)。
**为什么改**：原固定46对象仅机场走廊散布,世界空旷。程序化按相机位置动态生成连续城市,处处有楼,大幅提升代入感。
**影响范围**：新增worldgen.js(~50行);render.js(drawScenery重写+道路网格~25行);index.html(worldgen.js script);game.json(modules+0.14→0.15.0.0)。
**实测**：Playwright — per-cell确定性(同参cellsAround两次JSON完全一致)+592对象(vs旧46);连续铺展(远离机场z=8000处329对象/侧向x=3000处99对象,处处有楼);城市核137栋高楼(h>60);截图确认连续城市从近到地平线、跑道走廊留空;零console错误。注:592对象/帧需Tick4视锥剔除+LOD优化帧率。

## FA组13 Tick4 (渲染优化 视锥剔除+LOD+FPS · repo v0.16.0.0)
**改了什么**：程序化大量对象后保帧率:
- **视锥剔除**:render.js drawScenery每对象投影中心→屏外(c.x/c.y越界)或过小(屏幕尺寸<2.2px)→跳过,不绘worldBox。
- **距离LOD**:建筑按相机距离c.z分三级——近(<900,完整worldBox 5面+窗)/中(900-2200,5面无窗)/远(>2200,单前面fillPoly3d);树远(>1800)画点。
- **量化统计**:RSTATS{gen生成,drawn绘制,culled剔除};game.js FPS滑动平均(_fps);#fpsHud浮层显示"FPS+对象drawn/gen(剔culled)"(SYS.features.fpsHud开关gate,默认关)。
- **A/B开关**:OPT{cull,lod}(window暴露)供优化前后对比。
**为什么改**:Tick3程序化592+对象/帧worldBox(5面×裁剪)重,需剔除+LOD保帧率。auditor硬约束量化。
**影响范围**:render.js(RSTATS/OPT/drawBuilding LOD/drawScenery剔除~40行);game.js(FPS滑动平均+updateFpsHud);sys.js(features.fpsHud);index.html(#fpsHud);game.css(fpsHud样式);game.json(0.15→0.16.0.0)。
**实测**:Playwright A/B直接测drawWorld ms/帧(城市密集区665对象)——优化前6.05ms/帧(绘617)→优化后1.42ms/帧(绘459,剔206)=**4.26x提速**;剔除生效(绘459<生成665);FPS HUD显示"FPS 50 · 对象458/665(剔207)";截图世界仍丰富LOD远近不同;零console错误。

## FA组13 Tick5 (多跑道+滑行道网+机场扩展 · repo v0.17.0.0 · 组13收官)
**改了什么**：机场从单跑道27扩为多跑道布局:
- 新建 web/airport.js(避render膨胀):AIRPORT布局——副跑道[27R平行(cx-130)/33交叉(hdg330)]+5段滑行道+停机坪。rwPt(沿向/横向→世界坐标,按hdg旋转)+drawRunwayAt(任意位置/朝向跑道:面/中线/入口斑马/跑道号/边灯,全quad3d近平面裁剪)。AIRPORT.draw(T)画停机坪(含停机位线)+滑行道+副跑道(远近排序)。
- render.js drawAirport开头调AIRPORT.draw(T);主跑道27仍由drawRunway画(进近不破)。
**为什么改**：用户北极星高拟真,真实机场是多跑道+滑行道网,非单跑道。通用drawRunwayAt支持任意朝向(为侧风跑道/起降选择铺路)。
**影响范围**：新增airport.js(~45行);render.js(drawAirport调AIRPORT.draw 1行);index.html(airport.js script);game.json(modules+0.16→0.17.0.0)。
**实测**：Playwright — AIRPORT.runways[27R,33]+5滑行道;俯瞰截图见主27+副跑道+滑行道+停机坪在城市中;doReset后主进近跑道仍正常渲染(不破);世界非空;零console错误。

## FA组14 Tick2 (飞控电脑 FBW fbw.js · repo v0.18.0.0)
**改了什么**：新增 web/fbw.js 电传飞控:
- 正常法则NORMAL LAW:横滚=rate需求(满杆滚转,松杆**保持坡度不回中**,与直接法则自动回中本质不同);俯仰=load factor需求(松杆1g保持当前飞行路径角γ,调俯仰使γ回到保持值)。
- 包线保护:bank软限33(松杆自动回到)+硬限67°(满杆滚转停在67不再增);pitch限-15..30;α-floor失速保护(高迎角禁继续抬头+自动微下压);overspeed保护(超185kt自动抬头减速)。
- 降级:液压低(ctrlGain<0.6)或交流失电→degraded()→直接法则DIRECT(cascade)。
- game.js操纵:SYS.features.fbw开→经FBW.apply;关或降级→直接法则(原逻辑保留)。drawFMA显示NORMAL LAW(青)/DIRECT LAW(琥珀)。
**为什么改**：用户北极星高拟真飞控电脑。FBW真改操纵手感(坡度保持vs回中、包线保护)是A320级电传核心,非纯显示。
**影响范围**：新增fbw.js(~50行);game.js(操纵接FBW分支~12行+resetState重置+drawFMA法则显示);sys.js(features.fbw默认开);index.html(fbw.js script);game.json(0.17→0.18.0.0)。
**实测**：Playwright — 默认NORMAL;FBW满杆滚右4s→bank66.8°停硬限67(包线保护);roll20松杆1.5s→19.9°保持不回中(FBW);FBW关→17.3°自动回中(直接法则);液压故障3s→ctrlGain0.32<0.6→degraded+DIRECT;电气失效→DIRECT;零console错误。FBW真改手感可数值验证。

## FA组14 Tick3 (FMC航路点 + 仪表飞行IMC · repo v0.19.0.0)
**改了什么**：仪表气象与导航:
- 新建 web/weather.js(**只放visibility/ceiling基础态,Tick5 append气象**):WEATHER{visibility(m),ceiling(ft)}+preset(clear/mist/imc/lowvis)。drawIMC():云中(alt>ceiling)→全屏云灰白化(越深越浓);云底下低能见度→地平线/上方云幕雾化渐变(上浓下清,近处留可见),按visibility定浓度。
- render.js drawWorld末尾(HUD前)调drawIMC(覆于外景,PFD独立canvas不受影响=仪表飞行)。
- FMC航路点:render.js WPTS[IAF/FAF/RW27]进近序列,drawHUD显示下一航路点+距离NM+横向偏离XTK(中线偏差m L/R)。
- 天气可配:config.js CONFIG.weather+persist,飞行设置面板加天气seg(晴VMC/薄雾/IMC/低能见),applyConfig应用WEATHER.preset。
**为什么改**：用户北极星仪表飞行IMC。低能见度/云中靠PFD飞行是真实IFR核心,IMC真遮外景非纯显示。
**影响范围**：新增weather.js(~40行);render.js(drawWorld调drawIMC+drawHUD FMC航路点~8行);config.js(weather预设+persist+applyConfig);index.html(天气seg+weather.js script);game.js(天气seg接线);game.json(0.18→0.19.0.0)。
**实测**：Playwright像素采样 — 晴horizon[200,192,180]→IMC[209,209,210]偏云灰(雾遮外景);云中(alt984ft>云底700)中心[214,218,225]白化;PFD对比度82独立清晰(不受IMC,仪表飞行);HUD含FMC航路点+XTK;零console错误。注:雾为屏幕空间近似,后续可增per-object距离剔除增强。

## FA组14 Tick4 (空间迷向 spatial.js · repo v0.20.0.0)
**改了什么**：新增 web/spatial.js 前庭错觉:
- SPATIAL{leanRoll,somatoPitch,refLoss}+update(dt,S)。无外参照度refLoss评估:夜(cfg.tod night)+低能见/云中(WEATHER.visibility<3500/alt>ceiling)→refLoss↑(0..1)。
- 倾斜错觉leans:refLoss高时外景横滚错觉偏置leanRoll(缓慢游走~refLoss*9°,感觉水平实则缓坡);躯体重力错觉somatogravic:加速度→俯仰错觉somatoPitch(加速感觉抬头)。白天VMC/有参照→错觉消退归零。
- **核心命门**:render.js toCamera/drawWorld用「错觉有效姿态」(S.pitch+illPitch()、S.roll+illRoll())——外景世界视觉吃错觉倾斜;但drawPFD/drawAttitude(game.js)用真实S.roll/S.pitch不吃——PFD真实。跟外景"感觉"飞会偏,信仪表才正。
- SYS.env.spatialD开关(默认开,新手难度可关)。
**为什么改**：用户北极星空间迷向。真实飞行中无外参照时前庭错觉致迷失方向,必须信仪表——外景骗你/仪表真实的分叉是空间迷向核心,非纯显示。
**影响范围**：新增spatial.js(~40行);render.js(toCamera/drawWorld吃错觉偏置illRoll/illPitch);game.js(loop调SPATIAL.update+resetState重置);sys.js(env.spatialD);index.html(spatial.js);game.json(0.19→0.20.0.0)。
**实测**：Playwright — 夜+IMC守真实roll=0(飞行员按PFD保持水平)→错觉leanRoll6.43°/refLoss1,外景世界倾6.43°vs PFD真实0°(分叉6.4°,核心命门可测);白天VMC→错觉归零;spatialD关→无错觉;截图IMC白化外景+PFD姿态仪真实水平;零console错误。

## FA组14 Tick5 (高级气象 append weather.js · repo v0.21.0.0 · 组14收官)
**改了什么**：append weather.js(未覆盖visibility/ceiling/drawIMC)三类高级气象真连锁:
- **微下击暴流microburst**:triggerMicroburst()→WEATHER.step按相位驱动WIND.head:进(ph0-6)顶风增至12增升,出(ph6-16)顺风骤降至负+下沉气流压gamma(经典风切变事故);windshear ECAM(ph5-16)触发"WINDSHEAR·GO AROUND"。
- **尾流wake**:单向滚转冲击(先一侧后反向恢复,衰减)扰S.roll。
- **积冰icing**:SYS.env.icing开→ice累积120kg/s;增重AC.m=dryMass+油量+ice(协调fuel);**vStall()加积冰CLmax破坏(/1+ice/1500)→失速速度真升**;操纵效率hg×icePenalty(ice/3000降);antiIce除冰80kg/s;ECAM "ICING"。
- 修复:windshear/icing需在SYS.failures注册否则FAILURES.step的SYS同步会把weather触发的告警清掉。
**为什么改**：用户北极星高级气象。微下击暴流/积冰/尾流真连锁物理(WIND/AC.m/vStall/操纵)是硬核飞行核心,非纯显示。
**影响范围**：weather.js append(~40行);failures.js(windshear/icing注册);game.js(WEATHER.step入物理+hg×icePenalty+vStall积冰CLmax破坏+resetWx);sys.js(env.icing+failures.windshear/icing注册);game.json(0.20→0.21.0.0)。
**实测**：Playwright时序 — 微下击暴流head 3→11.7峰(ph6)→-0.1谷(ph>10)经典剪切+windshear ECAM(ph5-16 True);积冰4s ice486kg+AC.m62462+vStall 110→127.1kt(+17,CLmax破坏)+ICING ECAM;除冰6.5s ice→0+ECAM清;尾流单向roll11.7°;零console错误。

## FA组15 Tick2 (系统总控面板 systems.js · repo v0.22.0.0)
**改了什么**：新增 web/systems.js 系统总控面板("一切皆可开关"终极落地):
- PANELS第7面板"系统总控",build()遍历SYS四类(features/env/failures/panels)用SYS.list(cat)自动生成分组开关UI(29项),每项label+desc+toggle。
- wire:点开关行→SYS.toggle(已localStorage持久化)+applySysChange即时应用到运行态(turbulence→cfg.turb、sound→Sound、freeFlight→CONFIG、cleanHud→setHudClean、failures→FAILURES.trigger/clear连动、panels→PANELS.buildBar刷新tab、fbw/env逐帧读自动生效)。sync反映SYS真实状态。
- game.js暴露window.setHudClean供总控调;sys.js注册panels.systems。
**为什么改**：用户北极星"一切功能/故障/难度/环境皆可开关"。总控面板遍历SYS自动生成全部开关,集中一处管理,新增任何SYS项自动出现(零额外UI代码)。
**影响范围**：新增systems.js(~55行);game.js(window.setHudClean);sys.js(panels.systems);index.html(systems.js script);game.css(总控样式~12行);game.json(0.21→0.22.0.0)。
**实测**：Playwright — 7面板(系统总控tab);遍历生成29开关分4组;点turbulence→SYS True→False+cfg.turb即时应用;reload持久化保持;点engineFire故障开关→SYS+FAILURES.active双真连动;零console错误。

## 04:35 — FA组15 Tick3:MCDU 旗舰件 mcdu.js(v0.23.0.0)★用户北极星核心
**改了什么**:新增 PANELS 第 8 面板 MCDU(A320 风格飞控电脑 CDU),用户北极星"数十面板·超级拟真·MCDU 旗舰"落地核心件。
- 屏四周实体行选键:左 1L-6L + 右 1R-6R 共 12 个 LSK,纵向环绕 CDU 屏。
- 中央 CDU 绿屏:标题行 + 6 行(每行左/右标签 + 值),底部划写区(scratchpad)带光标。
- 下方功能键 12 个(INIT/F-PLN/DIR/PROG/PERF/DATA/RAD NAV/FUEL PRED/SEC F-PLN/ATC COMM/MCDU MENU/AIRPORT)4 列布局。
- QWERTY 字母键 A-Z(30 键含 / SP OVFY CLR)+ 数字键盘 0-9/./±(12 键)。
- 12 页注册表(MENU/INIT/FPLN/PERF/PROG/RADNAV/FUEL/DATA/DIR/SEC/ATC/AIRPORT),每页独立标题+6行内容。
- 交互闭环:字母/数字键→scratchpad 真回显;CLR 退格;功能键→翻页真切页;LSK→把 scratchpad 写入对应行(再点取回,行变青色 has)。
- 实时页(PROG/FUEL/F-PLN)逐帧 sync 读 S/FUEL/AC/WPTS 实时仿真值(高度/速度/航向/距RW27/油量)。
**为什么改**:用户原话"一个高级飞行计算机面板就是真的那种按钮只在屏幕左右和下面那种四周的按钮的超级拟真"。MCDU 是民航驾驶舱标志性飞控电脑,北极星三要素(高拟真·全功能·游戏性)的旗舰体现。
**影响范围**:新增 mcdu.js(~190 行);game.css(MCDU 样式~30 行);index.html(mcdu.js script,置 game.js 前);game.json(scripts 加 mcdu.js,0.22→0.23.0.0)。本 tick 仅布局+交互;字段语义闭环(scratchpad→LSK 写 WIND/航路)留 Tick4。
**实测**:Playwright — 面板 tab 现"MCDU"(8 面板);打开→12 LSK+30 字母键+12 数字键+12 功能键+CDU 屏全渲染;点 A/B/C→scratchpad 显"ABC"真回显;点 7/2/8→"ABC728";CLR→退一位"ABC72";点 1L→scratchpad 写入行(LSK 标 has 青);点 INIT/PERF/PROG→标题真切"INIT"/"PERF APPR"/"PROG"翻页;PROG 页含实时 ALT 读数;零 console 错误。截图确认 CDU 绿屏环绕键盘覆于飞行视图、底部 PFD 仍在(飞行继续)。

## 04:28 — FA组15 Tick4:MCDU 功能逻辑闭环(v0.24.0.0)
**改了什么**:让 MCDU 的 INIT/PERF/F-PLN 页字段从纯显示升级为真闭环(scratchpad↔LSK 双向 + 格式校验 + 写仿真)。
- 新增 MCDU.fmgc{costIndex,crzFL,vapp,wind} 存 FMGC 输入状态;页面 render 读它显已输入值(青色 ent)。
- 新增 MCDU_FIELDS 功能字段注册表(页面+LSK→{commit 解析校验, disp 已提交显示, clear}):
  - INIT 2L 成本指数(0-999)/ 3L 巡航高度(FL350|350,10-430,越界 ENTRY OUT OF RANGE)/ 4L 磁航向风(270/12)。
  - PERF 2L 进近速度 VAPP(80-220kt)。
  - 非法格式→msg='FORMAT ERROR' 拒收(保留 scratchpad 不清)。
- **写真仿真**:MAG WIND 输入解析风向/风速→写 CONFIG.windDir/windSpeed→调 applyWind() 重算 WIND.head/cross 顶/侧风分量(同 config.js 既有路径)+saveConfig 持久化。落地风场真随 MCDU 输入改变。
- F-PLN 页加 SPD/ALT 剖面列(IAF 210/3000、FAF 160/1500、RW27 140/13)+ 实时距离(z 差算)。
- lsk() 重构:功能字段走 commit 校验分支,非功能字段保留原通用 entries 写入/取回;LSK has 指示兼顾两者。
**为什么改**:Tick3 MCDU 是布局+回显骨架,本 tick 落地"飞控电脑真能改飞行参数"的闭环——北极星"全功能"要求 MCDU 非花瓶,输入真驱动仿真(如机组在 CDU 设进近风)。
**影响范围**:mcdu.js(+fmgc/MCDU_FIELDS/fieldDisp/lsk 重构/INIT·PERF·FPLN 页改,~80 行);game.json(0.23→0.24.0.0)。无新文件(已在 scripts)。
**实测**:Playwright 闭环验证(非纯显示红线):INIT 输"270/12"点 4L→WIND.head 真 4.05→6.17m/s(12kt 正顶风)+CONFIG.windDir/Speed 真写 270/12+该行青显"270°/12";输"ABC"点 LSK→scratchpad 显 FORMAT ERROR 拒收;PERF 输"145"点 2L→MCDU.fmgc.vapp===145+青显;越界"250"→ENTRY OUT OF RANGE;INIT 输"FL370"→crzFL===370 解析;F-PLN 含 SPD/ALT 列+IAF;零 console 错误。截图确认 INIT 页 MAG WIND 青色已输入值。

## 04:33 — FA组15 Tick5:新手分步交互教学 tutorial.js(v0.25.0.0 · 路线图收官)
**改了什么**:新增 TUTORIAL 状态机,7 步逐帧检测自动推进引导新手完成完整进近。
- 7 步覆盖全流程:开始进近(S.started)→对准中线(|x|<16 且 alt<760ft)→守下滑道+速度(|gsDev|<0.55°且|V-140kt|<15)→通过决断高度(alt<215ft)→入口拉平(phase=flare)→主轮接地(onGround)→反推刹停(reverse 且 V<35kt)。
- 每步 check(S)→bool 逐帧轮询,满足即自动进下一步(非点按钮),进步时 showCallout 绿色庆祝"完成·步N/7"。
- 浮层(#tutOverlay):顶部步骤进度+跳过按钮,中部当前步 title+hint,底部渐变进度条;Linear 深色风格与全局一致(复用 --cyan/--grn/--edge2 变量)。
- 启动入口:帮助模态加"新手分步教学"开关(swTut)+ SYS.features.tutorial 登记;开则 startFlight 时 TUTORIAL.start();可随时点"跳过教学"收起。系统总控面板亦可实时开关(applySysChange 接 window.setTutorial)。
- game.js loop 加 TUTORIAL.update(S,dt)(防御 typeof);gsDev 用与 drawPFD 同口径 atan2(alt,-z)*DEG-GS_DEG。
**为什么改**:北极星"游戏性"+用户②"确保新手能玩"。硬核全功能模拟器对新手门槛高,分步实时引导(满足条件自动前进、绿色庆祝、随时跳过)让零基础玩家也能走完第一次着陆。
**影响范围**:新增 tutorial.js(~95 行);game.css(浮层样式~16 行);index.html(script+帮助开关行);game.json(scripts+0.24→0.25.0.0);sys.js(features.tutorial);game.js(loop 钩子+startFlight 启动+swTut 接线+init 同步+window.setTutorial);systems.js(applySysChange tutorial 实时开关)。
**实测**:Playwright(自动推进非纯显示红线):开教学 SYS+startFlight→浮层显"步骤2/7 对准跑道中线"(步1 S.started 瞬时完成自动跳);脚本驱动 S.alt=200ft/S.x=3→等帧 loop 调 update→步真递增至 2/7"守住下滑道与速度"+进度条 29%;点跳过→浮层消失+TUTORIAL.active=false;零 console 错误。截图确认浮层卡片+绿色庆祝+城市场景+PFD 一致呈现。

—— 组15 收官:Tick2 系统总控(v0.22)/Tick3 MCDU 旗舰件(v0.23)/Tick4 MCDU 功能逻辑(v0.24)/Tick5 新手教学(v0.25)。原路线图组11-15 全部收官。

## 04:48 — FA组16 Tick2:多机型性能库 + 选型(v0.26.0.0)
**改了什么**:新增 AIRCRAFT 性能库,4 机型完整气动档案,一键切换真改飞行特性。
- 新建 aircraft.js:AIRCRAFT 注册表 4 机型——窄体(A320 级/双发/Vref140/基准)、宽体(B777 级/双发重型/Vref149/惯性大)、支线(CRJ 级/双发/Vref135/轻快)、货机(747F 级/四发最重/Vref154)。每档完整气动(m/dryMass/S/span/maxThrust/CLa/CL0/aoaStall/CD0/k/flapCL[]/flapStallBonus[]/flapCD[]/gearCD/spoilerCD/spoilerLiftLoss/gearHeight/revFactor/engines/vref/燃油容量)。
- applyAircraft(id):逐字段写 AC 全局对象(AC 是 const,只逐字段赋值不整体替换,避免气动计算持旧引用失效)+ ENGINES.setCount(机型发动机数 2/4)+ FUEL 三油箱容量按机型缩放(cap+qty 同步)。
- 起始进近速度按机型 vref 比例缩放(resetState:V=起始 V×vref/140),重机进近更快,防换大机型即失速——契合"确保新手能玩"。
- fuel.js reset() 改为 qty 复位到当前 cap(不再硬编码 5500/7000/5500),支持机型容量切换。
- config.js 加 CONFIG.aircraft(默认 narrow)+ applyConfig 调 applyAircraft + loadConfig 读取持久化;config modal 加机型 seg 选择器(.seg-opt[data-ac],显 Vref/重量)+ cfgAcVal 标签;game.js 选择器点击→applyAircraft+resetState+saveConfig+同步 UI。
**为什么改**:北极星"全功能·游戏性"+用户⑥"可配置多模式"。从单一机型扩为可选机型库,不同重量/推力/翼面/Vref 带来截然不同的操纵手感(轻快支线 vs 沉稳货机),大幅提升重玩价值与拟真度。
**影响范围**:新增 aircraft.js(~70 行);config.js(CONFIG.aircraft+applyConfig+loadConfig,~8 行);game.js(resetState vref 缩放+机型选择器 wire+syncConfigUI+updateAcUI,~20 行);fuel.js(reset 按 cap);index.html(机型 seg UI+script);game.json(scripts+0.25→0.26.0.0)。
**实测**:Playwright(切换真改气动非纯显示红线):窄体基准(dryMass44000/maxThrust180000/span34/2发/Vref140)→宽体(170000/660000/60/2发/149+燃油 cap30000)→货机(180000/1000000/64/4发/154+cap45000,发动机数真随机型变)→支线(21000/80000/24/2发/135)+起始速度真缩放至 69.43m/s(72×135/140)→切回窄体恢复 44000+速度 72→reload 后宽体持久化保持;cfgAcVal 标签同步;零 console 错误。截图确认机型选择器 4 档 Linear 深色一致。

## 04:56 — FA组16 Tick3:多机场航路库 + 目的地/进近选择(v0.27.0.0)
**改了什么**:新增 AIRPORTS 航路库,4 机场可选,切换真改跑道朝向/ILS/标高/航路点。
- 新建 airports.js:AIRPORTS 注册表 4 机场——维加国际 ZVGA(RWY27/110.30/120ft/基准)、阿尔卑斯山城 LSGA(RWY34/109.50/3120ft 高原陡下滑)、海岸都会 KSEA(RWY16/111.70/430ft 宽长跑道)、热带岛屿 RJTI(RWY05/108.90/28ft 短跑道)。每场含跑道号/朝向 hdg/长宽/ILS 频率/标高/PAPI 角/航路点。
- applyAirport(id):逐字段写 RWY 全局(W/L/elevFt/name/ils/hdg/papi/icao/aptName)+ PAPI.x 随跑道宽重算 + WPTS 原地改(length=0+push 保持同一数组引用,跑道阈值航路点 RW+号)。
- mcdu.js 解 3 处硬编码 ILS:RADNAV 页 ILS/FREQ 改读 'I'+RWY.name+'/'+RWY.ils + CRS 改读跑道朝向;AIRPORT 页 DEST/ELEV/LEN/ILS/CRS/PAPI 全读 RWY 当前值;新增 _rhdg/_ils/_rwy/_icao 读取器;_hdg 罗盘基准随跑道朝向。
- game.js:PFD 罗盘基准 baseH 从硬编码 270 改读 RWY.hdg;topbar badge(#apprBadge)+ config 标签(cfgApVal)动态反映当前机场;RWY 默认加 name/ils/hdg/papi/icao/aptName 字段。
- config.js:CONFIG.airport(默认 vega)+ applyConfig 调 applyAirport + loadConfig 读取持久化;config modal 加机场 seg 选择器(显 ICAO/跑道/标高)+ wire→applyAirport+resetState+saveConfig。
**为什么改**:北极星"全功能·游戏性"+用户⑥"可配置多模式"。从单一机场扩为可选目的地库,不同朝向/标高/跑道尺寸带来不同进近挑战(高原山城 vs 海岛短跑道),MCDU/PFD 全程读真机场数据,提升拟真与重玩价值。
**影响范围**:新增 airports.js(~45 行);mcdu.js(RADNAV/AIRPORT 页+读取器,~15 行);game.js(RWY 字段+baseH+updateAirportUI+选择器 wire+syncConfigUI,~20 行);config.js(CONFIG.airport+applyConfig+loadConfig,~5 行);index.html(机场 seg+badge id+script);game.json(scripts+0.26→0.27.0.0)。
**实测**:Playwright(切换真改非纯显示红线):vega 基准(RWY27/110.30/120ft/hdg270)→海岸(RWY16/111.70/430ft/hdg160/W60/L3500,PAPI.x 真重算-52,WPTS 末点 RW16,badge 真更新)→MCDU RADNAV 页显"I16/111.70"+AIRPORT 页显"KSEA RW16"+"430FT"→山城(34/109.50/3120ft/340)→切回 vega 恢复→reload 岛屿(05/28)持久化;零 console 错误。截图确认 MCDU AIRPORT 页动态机场信息。

## 05:05 — FA组16 Tick4:飞行轨迹回放系统(v0.28.0.0)
**改了什么**:新增 REPLAY 轨迹记录+复盘面板,着陆后可回看整段进近的下滑剖面与航迹。
- 新建 replay.js:REPLAY{buf,recording,REC_DT:0.5节流,MAX:400环形}。sample(S,dt)节流记录{t,x,z,alt,V,pitch,roll,hdg,gamma,phase,throttle};events()算关键事件索引(接地=首个rollout帧/最大坡度/最低速)。
- PANELS 第 9 面板"回放"(sys.js登记panels.replay):canvas 双图——上图侧视下滑剖面(alt vs 距离,叠虚线 3° 标准下滑道参考)+下图俯视航迹(横向 x vs 距离,叠中线±半跑道宽容差带);事件标记(红=接地/琥珀=最大坡度/青=最低速);底部 scrub 滑块拖动→高亮该时刻点+读数行(T+s/ALT/SPD/V·S/横偏/坡度)。无数据显"完成一次着陆后查看回放"。
- game.js:loop 节流调 REPLAY.sample;startFlight/doReset 调 REPLAY.start(开录);endGame 调 REPLAY.stop(停录);report 讲评加"看回放"按钮→PANELS.open('replay')。
**为什么改**:北极星"游戏性"+用户②"新手能玩"+⑤"完整体验"。落地后复盘下滑道/中线偏差,直观看到自己哪段偏高/偏左/速度掉,是飞行训练核心反馈环,大幅提升学习价值。
**影响范围**:新增 replay.js(~110 行);game.js(loop sample+start/stop 钩子+rReplay wire,~6 行);sys.js(panels.replay);index.html(看回放按钮+script);game.css(回放面板样式~10 行);game.json(scripts+0.27→0.28.0.0)。
**实测**:Playwright(真记录+真绘图非纯显示红线):9 面板(回放 tab);驱动 S 填 buf 40 帧;打开回放面板→canvas 真绘制(324 非空像素)+scrub 滑块(max39)+读数含 ALT/SPD/V·S;拖 scrub 中段→读数真变(T+19.5s/ALT7ft→T+6.5s/ALT142ft);endGame 后 report"看回放"按钮存在可见;零 console 错误。截图确认双图(绿下滑剖面+蓝航迹+容差带+事件点+scrub)。

## 05:12 — FA组16 Tick5:导航显示 ND 平面图面板(v0.29.0.0 · 组16收尾)
**改了什么**:新增 ND 导航显示面板,航向上 ARC 平面图实时投影航路全景。
- 新建 nd.js:PANELS 面板"ND 导航"(sys.js登记panels.nd)。canvas ARC 模式投影:世界向量(沿迹 dz/横向 dx)绕本机航向偏差 S.hdg 旋至航向上,按量程缩放到屏幕(本机绿三角靠下)。
- 绘制要素:航向刻度环(读 RWY.hdg+S.hdg,10°一刻/30°标数字+N/E/S/W,航向读数框)+ 同心量程圈(半/全,NM 标注)+ 跑道矩形(阈值→远端,有宽度)+ ILS 航道虚线(沿跑道轴延伸)+ 航路点 WPTS 投影(菱形+连线+标签 VEGAS/FAF/RW)+ 风矢量箭头(WIND.head/cross 合成风来向+风速)。
- 量程 10/20/40NM 三档面板内按钮切换(this.range),逐帧 sync 实时刷新读 S/WIND/WPTS/RWY。
**为什么改**:北极星"高拟真·全功能"。ND 是民航驾驶舱与 PFD 并列的核心导航仪表,给飞行员航路平面全局态势感知(自己在哪、航路点在哪、跑道方位、风),补齐驾驶舱拟真度,与已有 MCDU 形成"FMC 输入+ND 显示"闭环。
**影响范围**:新增 nd.js(~120 行);sys.js(panels.nd);index.html(script);game.css(ND 量程按钮/读数样式~10 行);game.json(scripts+0.28→0.29.0.0)。
**实测**:Playwright(真投影+量程真切非纯显示红线):10 面板(ND tab);打开→canvas 真绘制(456 非空采样点)+3 量程按钮;点 40NM→ND.range=40+按钮激活+重绘;距阈值读数随 S.z 真变(z=-8000→4.5NM、z=-2000→1.2NM 投影实时);切回 10NM;零 console 错误。截图确认航向环+ILS航道+航路点+风矢量+本机三角。

—— 组16 收官:Tick2 多机型(v0.26)/Tick3 多机场(v0.27)/Tick4 回放(v0.28)/Tick5 ND(v0.29)。22 模块 10 面板。

## 05:25 — FA组17 Tick2:FCU 自动飞行控制板 + AP 模式(v0.30.0.0)
**改了什么**:新增 AFS 自动飞行系统,独立于 EMMA 的可选模式自动驾驶 + FCU 控制板。
- 新建 afs.js:AFS{engaged,tgt:{hdgSel,altSel,spdSel,vsSel},modes:{lat:HDG/NAV/APPR,ver:ALT/VS/APPR,spd:SEL/MANAGED}}。PANELS 第 11 面板"AFS/FCU":AP 主开关+4 目标块(SPD/HDG/ALT/V·S,▲▼调,带环绕/限幅)+三组模式按钮(横向/纵向/速度)。
- AFS.update(dt):engaged 且 EMMA 非 control(让位优先级)时,按 FCU 模式算目标写 S——HDG 保持选定航向(算坡度)/ALT 保持选定高度(经 V/S 指令算俯仰)/VS 保持升降率/SPD SEL 自动油门到选定速度/MANAGED 锁 140/NAV·APPR 沿中线与 ILS;全权写 S.pitchIn/rollIn/rudder/throttle + 存 ap.fd 飞行指引。
- game.js loop:autopilot 后追加 AFS.update(STEP)(防御 typeof);AFS 内部 guard 保证 EMMA control 优先、地面/未起飞/已结束不接管。三态协调:EMMA control>AFS engaged>手动。
**为什么改**:北极星"高拟真·全功能"。真实民航 FCU(自动飞行控制板)是核心驾驶设备,玩家可选 HDG/ALT/SPD/V·S 分项自动 + NAV/APPR managed,介于全手动与 EMMA 全自动之间,与 ND/MCDU 形成完整自动飞行链。
**影响范围**:新增 afs.js(~130 行);game.js(loop AFS.update 钩子,1 行);sys.js(panels.afs);index.html(afs.js script,emma.js 后);game.css(FCU 样式~20 行);game.json(scripts+0.29→0.30.0.0)。
**实测**:Playwright(AP 真按 FCU 飞非纯显示红线):11 面板(AFS/FCU tab);打开→4 目标块+8 模式按钮+AP 主开关渲染;hdgSel ▲×3→270→285(步进 5° 真调);点 AP+HDG 模式→AFS.engaged=true&modes.lat=HDG;设右偏 20° 目标航向+EMMA off 驱动 5 帧→S.rollIn 真为正(右压坡度跟目标);ALT 模式设高目标→S.pitchIn 真为正(抬头爬升);EMMA control 时 AFS.update 返 false(让位);零 console 错误。截图确认 FCU 面板 AP 接通+目标块+模式行。

## 05:35 — 紧急修复:面板布局架构重构(v0.30.1.0)· 用户反馈手机版面板挡飞行视野
**改了什么**:把面板系统从「全屏覆盖飞行视野」重构为「飞行视野永不被盖 + 面板入下方仪表区切换」。
- 根因:`#panelHost` 原是 `#viewport` 子元素、`inset:0` 全屏盖住飞行视野(所有 11 面板共用此错误布局),手机上还溢出要左右/上下滑动。
- index.html:`#panelBar`(tab 条)移到飞行视野与仪表台之间;`#panelHost` 移入 `#console`(仪表区)。
- game.css:`#panelBar` 改 flex-wrap——12 个 tab 一屏全可见,绝不横向滚动;`#panelHost` 改 `position:absolute;inset:0` 覆盖 `#console`(仪表区)而非飞行视野,`overflow-x:hidden`(禁横滚)+ `overflow-y:auto`(仅密集面板纵向兜底);`#console` 加 `position:relative`;手机面板内容紧凑化(padding/字号);MCDU 键盘压缩。
- panels.js:open/close 给 `#console` 加/去 `panel-open` 类→面板打开时仪表区增高(手机 64vh),给面板腾空间,飞行视野压到约 36vh 但**始终完整可见**。
**为什么改**:用户原话「飞行视野是最不能挡的,下面的是可以通过看板切换的」「不要出现需要左右上下滑动才能看见面板全部按钮」。飞行视野是第一视角核心,绝不能被遮;面板属于「下方可切换看板」。
**影响范围**:index.html(面板系统 DOM 位置)、game.css(#panelBar/#panelHost/#console 布局 + 手机紧凑化 + panel-open 增高)、panels.js(open/close 切 console 类)、game.json(0.30→0.30.1.0)。架构级改动,惠及全部 11 面板。
**实测**:Playwright 手机视口(390×844,is_mobile,data-device=phone):tab 条 11 个 scrollW390=clientW390 无横滚;逐个打开 AFS/MCDU/系统总控/ND/故障→飞行视野始终可见(worldH 235px 在顶部)、面板在仪表区(hostTop 305≥飞行视野底)、**全部零横向滚动**、MCDU/AFS/ND/故障零纵向滚动(仅系统总控 29 开关纵向);返回飞行仪表恢复;零 console 错误。截图确认手机版 MCDU 顶部飞行视野+中部 tab+底部完整键盘无滚动。

## 05:39 — 布局修复补丁:4 发引擎面板手机横向溢出(v0.30.1.1)
**改了什么**:自检发现 4 发引擎面板在手机(390px)横向溢出(sw428>390,`repeat(4,1fr)` 被按钮内容撑开)。修:eng-grid 列改 `minmax(0,1fr)` 允许收缩;手机 4 发改 2×2 排布;eng-col 手机减 padding。
**为什么改**:延续上一条架构修复的「零横向滚动」铁律,主动自检其余面板(引擎/燃油/液压/电气/回放)发现引擎漏网。
**影响范围**:game.css(eng-grid 列定义 + 手机 2×2)。game.json 0.30.1.0→0.30.1.1。
**实测**:Playwright 手机视口复测:engine/fuel/hyd/elec/replay 全部 scrollW390=clientW390 零横向溢出;零 console 错误。

## 05:46 — FA组17 Tick3:ATC 空管语音通信 + 无线电面板(v0.31.0.0)
**改了什么**:新增 ATC 空管通信系统,按进近阶段自动发指令 + 语音 + 玩家应答。
- 新建 atc.js:ATC{log,fired,awaiting}指令状态机。step(S,dt)按 S.phase/alt 触发(每条去抖一次):放行(cleared to land RWY+wind)/进近继续(报距离)/风报更新/落地许可(check gear)/复飞(go around fly runway heading)/脱离跑道(vacate)。Sound.say 英文标准通话(读 RWY.name/CONFIG 风)。
- PANELS 第 12 面板"无线电"(sys.js panels.atc):TWR 频率+呼号+消息日志(时间戳 T+mm:ss、ATC 青/YOU 绿、word-break 换行)+应答按钮(WILCO/ROGER/SAY AGAIN→追加 YOU 条目+blip);sync 仅日志变化时重绘+滚到底。
- game.js loop 调 ATC.step(S,dt);startFlight/doReset 调 ATC.reset。
- **手机零横向溢出**(UI 铁律):日志 flex 纵列 word-break,应答按钮 flex-wrap flex:1。
**为什么改**:北极星"高拟真·游戏性"。空管通信是民航飞行沉浸感核心,塔台放行/落地许可/复飞指令 + 语音 + 应答,让进近不再孤单,显著提升临场感。
**影响范围**:新增 atc.js(~95 行);game.js(loop step+start/reset 钩子,3 行);sys.js(panels.atc);index.html(atc.js script);game.css(无线电日志样式~16 行,手机零横滚);game.json(scripts+0.30.1.1→0.31.0.0)。
**实测**:Playwright 手机视口(390×844,is_mobile):12 面板(无线电/ATC tab);开局 3 条指令真触发(ATC.log>0);面板 freq+日志+3 应答按钮渲染,**panelHost 零横向溢出**;点 WILCO→log 追加 YOU 'wilco' 条目;驱动 alt 降→落地许可(check gear, cleared to land)真出现;phase=goaround→go around 指令真触发;零 console 错误。截图确认手机版顶部飞行视野+ATC 日志(ATC 青/YOU 绿)+应答按钮无滚动。

## 05:53 — FA组17 Tick4:天气雷达 WXR 面板(v0.32.0.0)
**改了什么**:新增 WXR 气象雷达面板,扇形扫描显示程序生成的气象回波。
- 新建 wxr.js:PANELS 第 13 面板"气象雷达"(sys.js panels.wxr)。canvas 扇形(本机底部中心,向上±60°)极格回波场:确定性 hash(_wxrH01,按格 bi/ri+沿迹 seed)生成回波单元,强度 = 噪声²×天气强度 + 增益 − 距离衰减;绿(小雨)/黄(中雨)/红(强对流)三级。
- 天气强度由 WEATHER.visibility 推(晴空近乎无回波,IMC/低能见密集);量程 20/40/80NM + GAIN(CAL/NORM/MAX)+ TILT(DN/0/UP)三组面板按钮;扫描线左右往返动画(逐帧 sync 推进);量程圈+方位刻度+本机符号。
- **手机零横向溢出**(UI 铁律):canvas width:100%,量程/增益/倾角按钮 flex-wrap,图例 flex-wrap。
**为什么改**:北极星"高拟真·全功能"。气象雷达是民航驾驶舱核心,绕飞强对流的关键工具,回波随真实天气(WEATHER)联动,补齐恶劣天气下的态势感知。
**影响范围**:新增 wxr.js(~110 行);sys.js(panels.wxr);index.html(script);game.css(WXR 控件样式~12 行,手机零横滚);game.json(scripts+0.31→0.32.0.0)。
**实测**:Playwright 手机视口(390×844,is_mobile):13 面板(气象雷达 tab);打开→canvas 真绘制;WEATHER.preset('clear')→echoCount 0(晴空无回波),preset('imc')→echoCount 120(回波真增密);panelHost 零横向溢出;3 量程按钮,点 80NM→WXR.range=80;扫描线动画(连续帧 canvas 指纹不同 83343≠83219);零 console 错误。截图确认手机版扇形扫描+绿黄红回波(IMC)+量程/增益/倾角+图例无滚动。

## 06:00 — FA组17 Tick5:夜航/低能见度光环境系统(v0.33.0.0 · 跨层重构 D · 组17收尾)
**改了什么**:把散落 render.js 的灯光逻辑统一搬进 lights.js 集中门控,并新增 7 类灯种 + 机身灯首视反射 + 灯光透雾。
- **重构搬迁**(render.js→lights.js):drawApproachLights(进近灯 ALSF)+ drawRunway 边灯段逻辑搬进 LIGHTS._approach/_edge,render.js 移除 drawApproachLights 函数(实测 typeof drawApproachLights==='undefined' 确认真搬迁非原地)、drawRunway 删边灯循环;drawWorld 改调 LIGHTS.drawGround(T)。
- **新增 lights.js**:LIGHTS.state 10 灯开关 + PANELS 第 14 面板"灯光"(各灯 toggle + 时段 dusk/noon/night 切换)。
- **7 类新灯种**:RAIL 顺序闪光导轨(进近灯外段快速序列)/REIL 跑道识别灯(阈值两侧同步白频闪)/滑行道蓝边灯/机身航行灯(风挡下缘红左·绿右微光)/白频闪 STROBE(1.5s 双闪,雾夜反射全屏脉冲)/信标 BEACON(红闪)/着陆灯近地光锥(<320ft 前下方地面投光梯形 + 雾中眩光盘)。
- **灯光透雾**:_fog() 由 WEATHER.visibility 推,_glowK() 雾中光晕扩散(glow 半径 ×2.2);机身灯反射强度随雾/夜加成。LIGHTS 开关=总闸,T.lightBoost(时段)=强度系数。
- cfg.tod 纳入灯光面板(调 setTod)。
**为什么改**:北极星"高拟真"。夜航与低能见度光环境是民航沉浸感关键,把零散灯光逻辑收敛进单一模块便于扩展,补齐 RAIL/REIL/滑行道/机身灯/着陆光锥等真实灯种,雾夜中频闪反射 + 着陆灯眩光显著提升临场感。
**影响范围**:新增 lights.js(~145 行,含搬迁接收 ~40);render.js(drawWorld 调 LIGHTS+移除 drawApproachLights+删边灯段,净减~30);sys.js(panels.lights);index.html(lights.js script,render.js 后);game.css(灯光面板样式~12 行,手机零横滚);game.json(scripts+0.32→0.33.0.0)。跨 6 文件、3 层(UI/Render/Domain)。
**实测**:Playwright 手机视口(390×844):14 面板(灯光 tab);**drawApproachLights 已从 render.js 移除(typeof undefined,真搬迁确认)**;7 类新灯种方法(_rail/_reil/_taxiway/_landingCone/drawAircraftGlow)+搬迁方法(_approach/_edge)全在;10 灯种状态齐;面板 10 灯行+3 时段手机零横滚;切 night→cfg.tod=night;strobe 开关 True→False UI 同步;切 IMC→_fog 0.83(透雾升);零 console 错误。截图确认夜间 ALSF 进近灯走廊+rabbit+REIL/RAIL 频闪+边灯+入口绿灯密集灯阵。

—— 组17 收官:Tick2 FCU(v0.30)/布局重构(v0.30.1.x)/Tick3 ATC(v0.31)/Tick4 WXR(v0.32)/Tick5 夜航光环境(v0.33)。25 模块 14 面板。

## 06:08 — 紧急修复:信息密度致密化 + 引擎 EICAS 重设计(v0.34.0.0)· 用户反馈按钮过大/密度低
**改了什么**:按用户「信息密度极高、按钮做小」反馈,重设计引擎面板为高密度 EICAS + 全局控件致密化。
- 引擎面板重设计(panels.js):原 5 个全宽大按钮 → N1 主推力条+大号读数 + 6 项二级读数密排(N2/EGT/FF/**OIL P/OIL T/VIB** 新增 3 项,派生自 n2/egt)+ 5 个小控制 chip(STR/IGN/CUT/FIRE/PMP)。面板内容高 368px→197px(减 47%)却多 3 读数,chip 高 28px→20px。
- 全局致密化(game.css):tab 条/FCU/ATC/灯光/燃油/液压/电气/系统总控/WXR/ND/MCDU 各控件 padding+font 统一缩小;.eng-btn 通用按钮缩小(fuel/elec/hyd/failures 复用);.syspanel padding 收紧。玻璃座舱风:小控件+高读数密度。
- 面板打开仪表区高度下调(64vh→54vh 手机):内容致密后无需占满屏,飞行视野占比 28%→35%,面板不再「占 80%」。
**为什么改**:用户原话「信息密度要求极高不是面板占屏幕的80然后按钮老大个」「引擎转速这些内容呢」。真实玻璃座舱是密集小读数,原大按钮低密度违背本质。引擎 N1/N2/转速等核心参数现以 EICAS 密集呈现。
**影响范围**:panels.js(引擎 build/sync 重写,~30 行);game.css(引擎 EICAS 样式重写+全局致密化块~40 行+面板高度下调)。game.json 0.33→0.34.0.0。
**实测**:Playwright 手机视口(390×844):引擎面板 N1 条+6 读数+5 chip(chipH 20px)+滑油压 58 显示,内容 197px 无滚动;全 11 面板(afs/atc/wxr/lights/fuel/hyd/elec/systems/mcdu/nd/failures)零横向溢出;飞行视野占比升至 35%,MCDU 无横滚;零 console 错误。截图确认引擎 EICAS 密集呈现(N1 21% + N2 64/EGT 477/FF 0.8/OIL P 58/OIL T 113/VIB 0.1 + 小 chip)。

## 06:23 — FA组18 Tick2:SD 系统显示(只读 synoptic 多页)(v0.35.0.0)
**改了什么**:新增 SD 系统综合显示面板,6 页只读 synoptic 示意图 + 推力模式。
- 新建 sd.js:PANELS 第 15 面板"SD系统显示"(sys.js panels.sd)。**只读示意图**(区别于现有 HYD/ELEC/FUEL 控制面板——无控制按钮纯只读 sync):
  - HYD 页:三源 A/B/C 压力柱+PSI 数字(press×3000)+泵驱动标签(ENG1/ENG2/ELEC)+NORM/LO PR 状态;
  - ELEC 页:GEN1/GEN2 节点+AC/DC 汇流条+TRU+BAT 充电%+连接线通断色(绿带电/暗失电);
  - FUEL 页:三油箱油量柱+KG+泵节点+交输活门 OPEN/SHUT+总油量;
  - BLEED 页:ENG1/ENG2/APU 引气可用+PACK 增压;
  - ENG 页:二次参数表(N2/OIL P/OIL T/VIB/FF);
  - STATUS 页:INOP 失效设备一览(读 FAILURES.activeList+HYD 失压源+ELEC 失电 GEN+发动机失效/起火)。
- 页按钮循环切 synoptic,逐帧 sync 刷新压力/通断/流向(读 HYD.sys/ELEC/FUEL.tanks/ENGINES.list/FAILURES 实时态)。
- 推力模式徽标 CLB/MCT/TOGA/IDLE(由 S.throttle/S.phase 派生:复飞或地面高推=TOGA,>0.62=CLB,>0.18=MCT)。
- **高密度小字示意,手机零横滚**(页按钮 flex-wrap,grid minmax(0,1fr),小字)。
**为什么改**:北极星"高拟真·全功能"。真机座舱 ECAM/SD 与 overhead 控制面板并存——SD 给系统全局态势(管路通断/汇流条/流向),控制面板给操作。补齐这块拟真。
**影响范围**:新增 sd.js(~140 行);sys.js(panels.sd);index.html(script);game.css(SD synoptic 样式~40 行,手机高密度);game.json(scripts+0.34→0.35.0.0)。
**实测**:Playwright 手机视口(390×844):15 面板(SD系统显示 tab);6 页按钮+屏幕,手机零横滚;HYD 页含 PSI/HYD A;切 ELEC→AC BUS 内容真变;切 FUEL→XFEED/KG;HYD.fail('A')→press<0.5+屏幕"LO PR"真反映;STATUS 列 HYD A INOP;推力模式 CLB→TOGA 随 phase 变;零 console 错误。截图确认推力徽标+6 页+STATUS INOP。

## 06:30 — FA组18 Tick3:性能计算 PERF 页(v0.36.0.0)
**改了什么**:新增 PERF 性能计算面板,按机型/跑道/风/构型/温度算 V 速度与起降距离。
- 新建 perf.js:PANELS 第 16 面板"性能/PERF"(sys.js panels.perf)。计算(简化教学公式,**注明非真实 AFM**):
  - V 速度:Vstall=√(2·m·g/(ρ·S·CLmax))随质量/襟翼/标高/温度;V1=Vs·1.08、VR=Vs·1.15、V2=Vs·1.20(V1≤VR≤V2)、Vref=Vs着陆·1.3、Vapp=Vref+顶风附加。
  - 距离:起飞 TODR(能量法,maxThrust/质量净加速+顶风减地速)、着陆 LDR(气程+地面减速 2.6m/s²);跑道余量=RWY.L−需求(够绿/不够红变色)。
  - 密度修正:ρ 随 RWY.elevFt 标高 + OAT 温度。
- OAT ±5 步进调节;"应用 Vapp → 自动飞行"按钮**写 AFS.tgt.spdSel=Vapp + MCDU.fmgc.vapp 闭环**。
- 逐帧 sync 随质量(FUEL 消耗)/机型/风实时刷新。高密度读数网格(3 列 minmax),手机零横滚。
**为什么改**:北极星"高拟真·全功能"。起降性能计算是民航签派/机组核心工作,不同机型/重量/跑道/温度下 V 速度与距离差异巨大(宽体 270t 在 3000m 跑道起飞余量为负=起不来),给玩家真实的性能约束感知。
**影响范围**:新增 perf.js(~110 行);sys.js(panels.perf);index.html(script);game.css(PERF 网格样式~22 行,高密度);game.json(scripts+0.35→0.36.0.0);写 AFS/MCDU 闭环(perf.js 内)。
**实测**:Playwright 手机视口(390×844):16 面板(性能/PERF tab);12 读数 cell+应用按钮,手机零横滚;窄体(GW62000/Vref141/LDR1280)vs 宽体(GW270000/Vref163/LDR1605)质量/速度/距离真增;DOM 随机型刷新;OAT 15→30→Vref 真变;点应用→AFS.tgt.spdSel==算得 Vapp(175)闭环;宽体起飞余量 -1433 红色警示;零 console 错误。截图确认起飞/着陆双段读数网格+余量变色+应用闭环。

## 06:37 — FA组18 Tick4:电子检查单 ECL(v0.37.0.0)
**改了什么**:新增 ECL 电子检查单面板,正常+非正常检查单,自动感知+手动勾选。
- 新建 checklist.js:PANELS 第 17 面板"检查单"(sys.js panels.ecl)。检查单库 6 套:正常(进近 APPROACH/着陆 LANDING/复飞 GO-AROUND)+非正常(ENG FIRE/ENG FAIL/HYD LO)。
- 每项{t,sense?(S)→bool}:有 sense 自动感知绿勾(起落架由 S.gearDown、襟翼由 S.flaps、扰流板由 S.spoilerArmed、复飞推力由 S.throttle、灭火手柄由 ENGINES.fuelCut),无 sense 手动点击勾选。
- recommend()按 FAILURES.active(起火→ENGFIRE/失效→ENGFAIL/液压→HYDLO)+ S.phase(复飞→GOAROUND)+ alt(<800ft→LANDING)自动推荐当前单;"自动"开关 + 单名 tab 可手动切换。
- 完成度进度条(已勾/总数)。逐帧 sync 刷新自动感知+荐单;签名缓存(内容不变跳过 DOM 重建,避免逐帧重建致点击失效)。高密度小行,手机零横滚。
**为什么改**:北极星"高拟真·游戏性"+用户②"新手能玩"。电子检查单是民航机组标准操作纪律,自动感知减负+非正常单应急处置引导,提升操作规范感与新手友好。
**影响范围**:新增 checklist.js(~120 行);sys.js(panels.ecl);index.html(script);game.css(ECL 列表样式~26 行,高密度);game.json(scripts+0.36→0.37.0.0)。
**实测**:Playwright 手机视口(390×844):17 面板(检查单 tab);7 tab(自动+6 单)+项列表+进度,手机零横滚;S.gearDown=true→起落架项自动绿勾、收上→不勾(自动感知真);手动项点击→勾选 toggle;FAILURES.trigger('engineFire')→自动荐 ENGFIRE 单(active 切换);ENGINES.fuelCut+火熄→灭火手柄/灭火瓶项自动勾;签名缓存修复点击稳定性;零 console 错误。截图确认着陆单 2/5+自动绿勾+手动项。

## 06:44 — FA组18 Tick5:故障情景训练 + 处置评分(v0.38.0.0 · 组18收尾)
**改了什么**:新增 SCENARIO 故障情景训练面板,预设剧本注入+处置监测+评分 debrief。
- 新建 scenario.js:PANELS 第 18 面板"训练情景"(sys.js panels.scn)。4 剧本:发动机起火(engineFire)/单发失效(engineFail)/液压全失(hydFail)/风切变进近(WEATHER.triggerMicroburst)。
- SCENARIO 状态机:start(id)选剧本→清残留故障+重置;step(S,dt)按高度(injAlt)注入对应 FAILURES.trigger;监测玩家正确处置(起火→ENGINES.fuelCut 灭火手柄/失效→蹬舵 |rudder|>0.15/液压→构型或复飞/风切变→TOGA 复飞)+响应时间(注入到正确动作间隔);情景结束(phase=ended/超时 60s)→评分(基础 40+正确处置 40+响应快 ≤20)+debrief 时间线(事件 T+时刻)。
- game.js loop 加 SCENARIO.step(S,dt)钩子(同 ATC/FAILURES.step 链);情景由面板 start/重置 控制。签名缓存避免逐帧重建。高密度 2×2 剧本卡+时间线,手机零横滚。
**为什么改**:北极星"高拟真·游戏性"+用户②"新手能玩"。故障情景训练把孤立故障升级为「注入→监测处置→评分复盘」的训练闭环,教玩家正确应急处置(失火拉手柄、风切变果断复飞),提升技能与可玩性。
**影响范围**:新增 scenario.js(~110 行);game.js(loop SCENARIO.step 钩子,1 行);sys.js(panels.scn);index.html(script);game.css(情景/debrief 样式~26 行,高密度);game.json(scripts+0.37→0.38.0.0)。
**实测**:Playwright 手机视口(390×844):18 面板(训练情景 tab);4 剧本卡+重置,手机零横滚;选起火+alt<700→注入 FAILURES.engineFire.active=true(剧本注入真);ENGINES.fuelCut→_handled=true(正确处置记录);phase=ended→评分 99+4 事件 debrief+DOM 显"评分/处置";重置→active=null;零 console 错误。截图确认 4 剧本卡 2×2+状态区。

—— 组18 收官:Tick2 SD(v0.35)/Tick3 PERF(v0.36)/Tick4 ECL(v0.37)/Tick5 情景训练(v0.38)。28 模块 18 面板。

## 07:00 — FA组19 Tick2:着陆履历 + 成就系统(v0.39.0.0)
**改了什么**:新增 HISTORY 履历面板,localStorage 持久化着陆评分历史+统计+成就。
- 新建 history.js:PANELS 第 19 面板"履历"(sys.js panels.hist)。HISTORY.record({score,grade,fpm,tdz,xoff})自动补机型/机场/时间→存 localStorage('fa.hist' 环形 50)。
- 面板三区:①统计(总着陆/均分/最佳/奶油率<150fpm 占比)②成就网格 6 类(初次着陆/A级着陆/奶油落地<150fpm/飞遍4机型/到访4机场/连续3次A,达成绿亮+未达暗显)③着陆记录列表(等级色/分/机型·机场/时间,倒序)。
- game.js showReport 正常着陆(grade A-E)+ crashedReport(grade F)末调 HISTORY.record。签名缓存避免逐帧重建。高密度小行,手机零横滚。
**为什么改**:北极星"游戏性"。履历+成就把单次着陆升级为长期进度追踪与目标驱动,提升重玩动力(刷 A 级/集齐机型机场/连续高分)。
**影响范围**:新增 history.js(~80 行);game.js(showReport×2 钩子,2 行);sys.js(panels.hist);index.html(script);game.css(履历/成就样式~30 行,高密度);game.json(scripts+0.38→0.39.0.0)。
**实测**:Playwright 手机视口(390×844):19 面板(履历 tab);模拟 5 次着陆→列表 5 项+统计(均分 94/最佳 97/奶油率 80%)真算;成就初次/A级/奶油/连续3A 解锁;reload 后 5 记录持久化(localStorage);手机零横滚;零 console 错误。截图确认统计+成就网格+记录列表。

## 07:05 — FA组19 Tick3:幽灵轨迹对比(v0.40.0.0)
**改了什么**:新增 GHOST 幽灵轨迹系统,飞行视野叠加该机场历史最佳进近线供追逐对比。
- 新建 ghost.js:GHOST{best{},enabled,maybeSave,draw}。着陆评分超该机场历史最佳→序列化 REPLAY.buf 为轻量 {z,alt,x} 存 localStorage('fa.ghost' 按机场 key,仅存更优)。
- draw():render drawWorld 末调,**复用 render 既有 project(world→screen)投影**(auditor 强制,与真实世界投影一致):画幽灵航迹青色虚线(各点 project 连线)+ 本机前方 150m 处幽灵机标记(圆+十字,按 _interp 插值最佳线在该距离的 x/alt)。
- PANELS 第 20 面板"幽灵"(sys.js panels.ghost):叠加开关(enabled)+当前机场最佳分显示+清除按钮。
- game.js showReport 末调 GHOST.maybeSave(RWY.aptName,REPLAY.buf,score);render.js drawWorld 末加 GHOST.draw()(enabled 时)。
**为什么改**:北极星"游戏性"。幽灵轨迹是竞速/训练经典机制——追逐自己的最佳线,直观看到本次 vs 最佳的高度/横向差异,提升精进动力。区别于回放(着陆后静态复盘),幽灵是进近中第一视角实时跨次对比。
**影响范围**:新增 ghost.js(~95 行);render.js(drawWorld 调 GHOST.draw,1 行);game.js(showReport 钩子,1 行);sys.js(panels.ghost);index.html(script);game.css(幽灵面板样式~16 行);game.json(scripts+0.39→0.40.0.0)。
**实测**:Playwright 手机视口(390×844):20 面板(幽灵 tab);开关/清除/最佳分区,手机零横滚;maybeSave(90)→存(curBest 90),低分 70 不覆盖,高分 96 覆盖;开启叠加→切回飞行 drawWorld 调 GHOST.draw 48 帧(复用 project 无错);面板显最佳分 96;清除→curBest null;零 console 错误。截图确认叠加开关+最佳分区+清除。
